interests.db
__pycache__/
*.pyc

# Local tooling
.claude/

# Legacy site — node_modules and build artefacts should never be committed.
astro/node_modules/
astro/dist/

# westminster-interests/src/data/ is generated by export.py but committed
# to the repo so Vercel (and any other build environment without Python +
# interests.db) can build the static site. Regenerate locally with:
#   py import.py && py rebuild_master.py && py seed_appgs.py && py export.py
# then commit the diff.
